{
  "name": "sanctuary-unit-db",
  "version": "0.1.0",
  "private": true,
  "description": "Unit database for Sanctuary: Shattered Sun, generated from the game's Lua templates",
  "type": "module",
  "//": "No `build` script on purpose. Vercel auto-detects and runs `npm run build` when one exists, and these scripts need a local game install. Deploys serve public/ as-is.",
  "scripts": {
    "extract": "node scripts/extract.js",
    "icons": "node scripts/build-icons.js",
    "refresh": "npm run extract && npm run icons",
    "verify": "node scripts/verify.js",
    "dev": "node scripts/serve.js"
  },
  "engines": {
    "node": ">=18"
  }
}
