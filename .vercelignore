# The build needs src/, public/ and the npm manifests. Everything below is
# local tooling that reads the game install, or source material for it — none
# of it exists or runs in production.
scripts/
icons-src/
README.md
node_modules/
.vercel/
.claude/
dist/
.DS_Store
